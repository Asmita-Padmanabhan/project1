class City {
  final String cityName;

  City({required this.cityName});
}

  // If you are fetching from JSON, add a factory constructor:
  //factory City.fromJson(Map<String, dynamic> json) {
    //return City(
      //cityName: json['cityName'] as String,
    //);
  //}
//}
